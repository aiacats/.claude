# 実装時の基本方針

## 1．実装前の原則

- 「.claude」フォルダ内の「claudeLog.txt」というファイルを読み込んで、過去の自分の作業を把握する。
  ファイルが大きい場合は全読みせず**末尾の直近エントリ（目安200行）のみ**読む。1000行を大きく超えていたら古い分を `claudeLog-archive.txt` へ移すローテーションを提案する。
- バグの修正を依頼した場合、ルート直下に存在する「log」フォルダ内の最新ログファイルを参照して原因を確認する。
- 既存コードを調査する。
    - 関連する機能の実装内容を把握する。
    - 使用されているライブラリ・フレームワークを把握する。
    - プロジェクトの命名規則とコーディングスタイルを把握する。
- requirements.txt, package.json, CMakeLists.txt 等で依存関係を確認する。
- 実装を行う前に実装手法を提案し、その承諾を得てから実装を行う。

## 2．コード実装時の原則

- Planの提案と破壊的な変更以外は全て自動で承認して実装を進める。
- 実装を簡易版で済ませることをせず、大規模な実装になるとしてもこちらの要求を完全に満たすために必要な本格的な実装のみを行う。
- 既存コードのコーディングスタイル・命名規則・設計パターンに従う。
- 既存のライブラリで目的の達成に有用なコードが既に実装/API が存在する場合、可能な限り既存のコードを用いて実装する。
- 実装にあたって必要なライブラリが存在する場合、インターネット上からそれをインストールするようにする。
  ClaudeCodeだけでのインストールが難しい場合、インストール方法を説明し、こちらにインストールをさせる。それが完了するまで実装は一時的に停止する。
- 必要最小限の変更で目的を達成する。
- 必要とされる精度は維持した上でパフォーマンスを重視する。
- UI に関連する処理は非同期にし、メインの処理中にキャンセルなどの動作が出来るようにする。
- 堅牢かつメンテナンス性に優れた状態になるように、SOLID 原則・DI を意識しつつ適切なデザインパターンも取り入れる。
- 新規にファイルを作成する際、適切なディレクトリが存在するか確認する。無い場合には適切な場所に適切なディレクトリを生成して、そこにファイルを作成する。
- 説明文書などのドキュメントは明示的に要求した時にのみ作成する。
- API キーやパスワード等をコードに含めない。
- 実装の過程で不要になったコードやファイルが発生した場合には該当コード・ファイルを削除する。
- 一つのクラスが 500 行を大きく超えた場合には責任及びクラスの分割を検討する。
- 可能な限りコンパイルエラーが発生しないよう、他の関連クラスとの関係性も確認しながら実装を行う。
- エラーの修正は、応急処置的な対応はせず根幹の問題を確認して、その問題に対して解決をするようにする。
- **一時回避策に逃げない（恒久・ユーザー強調）**: 問題に当たったら、回避策を積み重ねるのではなく「本来直すべき箇所」を
  最優先で特定して直す。回避策は根本原因の調査を遅らせ、技術的負債を増やす。確認手段(キャプチャ等)が無いなら
  「確認手段そのものを正しく用意する」方を優先する。
  例(2026-06-01): UIが崩れて見えた真因は UXML に USS を未リンクだったこと、確認できなかった真因は WinGui MCP の
  focus/capture 未整備だったこと。offscreen RT や focus ハックで凌ぐ前に、これらを直すべきだった。
- フォールバック（Fallback）の実装は行わない。
- コンソール出力するログはルート直下に存在する「log」フォルダ内に「yymmdd-hhmmss.log」という名称でテキストファイルにも保存するようにする。
  その実装が行われていない場合にはその実装を行う。
- 実装を中途半端に止めず、実装する予定のものは全て実装を行う。

## 3．実装後の原則

- 各プロジェクトの使用言語・フレームワークに沿ったやり方（後述参照）でコンパイルエラーなどが発生していないかを確認する。
  発生していた場合には、エラーが無くなり正常にアプリを実行出来るようになるまで修正を行う。
  エラー修正の際には、想定していた実装が実現出来ることを維持して、なおかつ応急処置的な対応ではなく根本原因を解決する。
- **Unityプロジェクトの場合**、Unity 固有ルール（`~/.claude/rules/unity.md`、SessionStart hook が自動注入）のデバッグフローに従う。

### 検証コール頻度の原則（Unity に限らず一般化）

長時間ループや per-edit の自動検証は context・ログ・コストを無駄に消費する。以下の原則を守る。

- **編集 → 即検証を毎回回さない**。複数ファイルにまたがる修正をひとまとめに行ってから1回検証する（バッチ検証）。
- **状態確認は polling せず "完了待ち" 系のブロッキング API を優先する**。Unity の `wait_for_compilation_done`、CI なら `gh run watch`、ビルドなら同期実行など。
- **どうしても polling が必要な場合**、最初は粗い間隔（例: 10〜20秒）で開始し、必要に応じて短縮する。2秒間隔の連打は禁止。
- 長時間待ちは `Bash(... run_in_background: true)` で投げて、自分の作業を止めない。
- 実装後に、以下の報告を最後に行う。
    - 問題が発生し、その修正を行った場合には、発生していた問題点
    - 実装内容の概要
    - 実装した内容についての潜在的問題・懸念点
- 上記の報告内容を、「.claude」フォルダ内の「claudeLog.txt」というファイルに時系列と共に追記していく。
  - **claudeLog.txt への書き込みは常に許可されている。事前確認は一切不要で、Edit ツールで直接追記してよい**（全プロジェクト共通の恒久ルール）。

### 実装後の自主レビュー（必須）

コーディング作業（複数ファイル変更・新機能追加・バグ修正）が一段落したら、**ユーザーからレビュー指示がなくとも必ず自主レビューを実施する**。これは「ユーザーが見直しを依頼しなくても、Claude が自発的に整合性をチェックする」というワークフローを徹底するための恒久ルール。

レビュー手順:
1. **変更全体の整合性確認** — 修正した全ファイルを通読し、相互依存（namespace解決・型参照・呼び出しチェーン）が壊れていないか確認
2. **静的レビュー** — 規模が大きい（3ファイル以上 or 100行以上）場合はサブエージェント（general-purpose）を起動して独立レビューを依頼。プロンプトに「コンパイル可否・cross-file consistency・SyncVarやライフサイクル順序の問題」を明示
3. **検証コマンド実行** — プロジェクトの言語/フレームワークに沿ったやり方でコンパイル・型チェック・テストを実行
    - Unity: `~/.claude/rules/unity.md` のデバッグフローに従う
    - C++: ビルド実行 → エラー確認
    - その他: lint/format/test を1回ずつ
4. **発見した問題は即修正** — レビューで指摘された潜在的問題（リーク防止・try/finally・null安全性等）は同じ作業ブロック内で対応する
5. **報告に含める** — 「自主レビューを実施し、N件の追加修正を行った」と明示

このレビューを省略するのは、変更が1ファイル数行のtypo修正のような自明なケースのみ。判断に迷ったらレビューする。

### コミットと README

- 適度な粒度の機能が実装出来たと判断したタイミングでローカルリポジトリへ適切な粒度の複数コミットに分解してコミットする。細かすぎる必要はない。Git情報がない場合、Initializeを行う。
    - Submoduleへのコミット・PRなど書き込み処理は絶対にしない。
- 更新内容をGitのRootに存在するREADME.mdに反映させる。存在しない場合は作成する。
  （これは「ドキュメントは明示要求時のみ作成する」原則の明示的な例外。README.md と claudeLog.txt は要求がなくとも常に更新してよい）

## 4．バグ検証・GUI検証の原則

### バグ検証の一般原則

- **再現ファースト**: 修正前に再現手順を確立する。再現できないバグは「直った」と判定できない。再現手順は claudeLog.txt に記録する。
- **仮説→計測→修正**: 推測で直さない。ログ・デバッガ・キャプチャで真因を裏付けてから直す。調査用に仕込んだ一時ログは修正完了後に削除する。
- **証拠付き報告**: 「直りました」にはテスト結果・ログ・スクリーンショットのいずれかの証拠を添える。証拠が出せない場合は「未検証」と明示して報告する。
- **同一手順で再検証**: 修正後、確立した再現手順をそのまま再実行して消えたことを確認し、隣接機能への副作用も最小限チェックする。
- **原因コミットの特定**: 「いつからか壊れた」系のバグは `git bisect` / 二分探索を第一手にする。

### GUI検証の原則

- **視覚バグは視覚で検証する（必須）**: コードを読んで「直ったはず」で報告しない。キャプチャを取得し、自分で画像を読んで確認するまでが修正。

  | 対象 | キャプチャ/検証手段 |
  |---|---|
  | Unity Editor | unity-mcp `screenshot` |
  | Windows アプリ (.exe) | WinUITestKit（第一選択。導入手順は `memories/winuitestkit_tool.md`）/ win-gui `capture_window` |
  | Web | playwright |

- **Before/After キャプチャ**: レイアウト修正は修正前後のキャプチャを撮って比較する。確認後の画像ファイルは削除する。
- **キャプチャ手段が無ければ、修正より先に手段を整備する**（2026-06-01 の教訓）。目視できないまま推測修正を重ねない。
- **目視よりアサーション**: `find_text` / `wait_for_text` / `get_pixel_color` / 画像 diff で機械的に判定できるものはそちらを優先する。
- **UIテスト必須**: GUI アプリの実装完了時は UI テストを必ず実行し、PASS + キャプチャの視覚確認をもって初めてコミット・報告する。新機能を実装するたびに、その機能を検証するテストステップを必ず追加する。

### レイアウトバグの調査順序（チェックリスト）

1. **スタイル未適用を最初に疑う** — スタイルシートのリンク（UXML↔USS、ResourceDictionary 等の参照）→ セレクタ優先度・継承 → 計算済みスタイル、の順に確認する。
2. **サイズ起因** — アンカー/ピボット/flex 設定、min/max サイズ、親コンテナの制約。
3. **環境起因** — ウィンドウリサイズ、解像度・アスペクト比、DPI（4K ディスプレイでは `SetProcessDPIAware` 必須）。
4. **コンテンツ起因** — 長文/日本語文字列のオーバーフロー・クリッピング、空データ。
5. **状態網羅** — 初期/ローディング/エラー/空/大量データの各状態でキャプチャ確認する。

## 5．トラブルシュート知見の蓄積

何度か詰まった問題が発生して解決した場合、Notionナレッジベースに蓄積する。
トラブル発生時は、まず既存の知見を検索してから調査に入る。

- 送信: `~/.claude/tools/notion-knowledge.sh <タイトル> <Technology> <Project> <問題詳細> <Solution>`
- 読込: `~/.claude/tools/notion-knowledge-read.sh list [Technology]` — 一覧表示
- 読込: `~/.claude/tools/notion-knowledge-read.sh search <keyword>` — キーワード検索
- 読込: `~/.claude/tools/notion-knowledge-read.sh read <page_id>` — ページ詳細
- 設定ファイル: `~/.claude/.notion-config`

### 能動的な知見化ルール（ユーザー指示を待たない）

- **Notion への登録は Claude の判断で勝手に行ってよい（事前確認・提案は不要）**。ユーザー恒久許可（2026-06-13）。
  価値ある知見（後述の判断目安に合致）が得られたら、`notion-knowledge.sh` で即登録する。登録後に「○○を Notion 登録しました」と事後報告する。
- **同じ問題で2回以上詰まって解決したら、その時点で `notion-knowledge.sh` で登録する**（許可を求めない）。
- **トラブル発生時は、調査開始前に必ず `notion-knowledge-read.sh search <keyword>` を実行する**。既存知見の見落としを防ぐ。
- **セッション末**: そのセッションで得た知見が1件以上あれば、判断目安に合致するものは自動で登録し、事後報告する。
- 候補判断の目安: ライブラリ仕様の罠、再発しうる環境構築の落とし穴、複数手順を経て解決した問題、ドキュメント化されていない暗黙の制約。

## その他の原則

- 破壊的な変更（リポジトリへの Push・削除行為等）を除き、確認を挟まず自動で進める（ツール許可の実制御は settings.json の permissions が担保）。
- 会話は全て日本語で行う。

## プロジェクト固有 `CLAUDE.md` の運用方針

セッション冒頭で**プロジェクトのGitルートに `CLAUDE.md` が存在するか確認**する。無ければユーザーに「プロジェクト固有の `CLAUDE.md` を作成しますか？」と提案する（特に新規プロジェクト・初回セッション時）。

プロジェクト固有の文脈はコードから読み取れない／毎セッションの再発見コストが高いため、明文化することで精度と速度が大きく向上する。

### プロジェクト `CLAUDE.md` に含めるべき項目

1. **言語ごとの検証コマンド** — コンパイル / lint / format / test を1コマンドで再現できる形で列挙
2. **アーキテクチャ要約** — 主要ディレクトリの責務、エントリポイント、asmdef/モジュール境界、依存方向
3. **ドメイン知識** — コードに現れない業務・運用ルール（VTuberライブなら「Day1/2のシーン構成」「OSC信号経路」「タイムライン依存関係」等）
4. **触る前に確認が必要な領域** — 本番設定、外部APIキー周辺、ライブ本番系コード、不可逆なマイグレーション
5. **過去の重大インシデントの根本原因と防止策** — 同じ事故を繰り返さないための明示
6. **このプロジェクトでよく使うMCPサーバー一覧** — 後述の「MCP プロジェクト別有効化」と連動

### 運用ルール

- 既存の `CLAUDE.md` がある場合、新規作業を始める前に必ず読む。
- 作業中に「これプロジェクト `CLAUDE.md` に書いておくべきだ」と感じた事項があれば、ユーザーに追記提案する。
- 雛形作成は `/init` skill が使える。

## サブエージェントの活用方針

メインのコンテキスト窓を保護するため、以下のパターンは積極的にサブエージェント（`Agent` ツール）に委譲する。直接 Grep/Read を多発させると context が汚れて精度が落ちるため、**目安「3クエリ以上の探索 = 即サブエージェント」**を意識する。

| 用途 | 委譲先 |
|---|---|
| 広域探索 / "どこにXがあるか" 系 | `Agent(subagent_type="Explore")` |
| 大規模変更前の設計判断 | `Agent(subagent_type="Plan")` |
| PR前の独立検証 | `/review` または `/security-review` skill |
| 完全独立な並列作業 | `run_in_background: true` で複数 Agent 並列起動 |

サブエージェント呼び出し時の原則:
- self-contained なプロンプト（前提・目的・期待形式・字数上限を明示）を渡す。
- 単純な探索や整形作業は `model: "haiku"` を明示してコスト・速度を最適化する。
- メインのClaudeはオーケストレーションに徹し、サブエージェントの結果を要約して受け取る。
- サブエージェント結果は鵜呑みにせず、コード変更が含まれる場合は実差分を必ず確認する。

## MCP サーバーのプロジェクト別有効化方針

グローバルに大量のMCPサーバーを有効化すると、毎セッション冒頭で deferred tools が大量に並び context を圧迫する。**プロジェクトごとに必要なMCPのみ** `.claude/settings.local.json` の `enabledMcpjsonServers` に列挙する。

| プロジェクト種別 | 推奨有効化 |
|---|---|
| Unity | `claude-code-mcp-unity`, `context7` |
| Web/フロントエンド | `playwright`（あれば）, `context7` |
| データ分析・Notebook | `ide`（Jupyter executeCode）, `context7` |
| ドキュメント・調査 | `context7` のみ |

**`win-gui` のような暴発リスクが高いMCPは常時有効化しない**。必要時のみ追加し、用が済んだら外す。

過剰に有効化していると感じた場合（context圧迫・無関係ツールの大量表示等）、ユーザーに「このセッションで使わない MCP を一時的に外しますか？」と提案してよい。

## MCPサーバーがセッションに公開されない時のチェックリスト

`.mcp.json` を配置済みでも、セッション冒頭でMCPツールが見当たらない・呼び出せない場合は以下を順に確認する（過去に複数プロジェクトで同じ詰まり方を再発させているため必読）。

1. **`enabledMcpjsonServers` が抜けていないか**
   - プロジェクト直下の `.claude/settings.local.json` のトップレベルに以下が必要:
     ```json
     "enabledMcpjsonServers": ["<サーバー名>"]
     ```
   - `.mcp.json` 登録 + `claude mcp list` で「✓ Connected」表示でも、これが無いとツールはセッションに公開されない（プロジェクトスコープMCPはユーザー明示承認を要する安全機構のため）。
   - **未設定なら即追記してClaude Codeを再起動する**。ユーザーに確認せず追記してよい。

2. **deferred tools（遅延ロード）の仕様と混同しない**
   - 上記設定済みでもセッション冒頭ではツールスキーマが未ロードの「deferred」状態で出ることがある。これは仕様であり抑止不要。
   - 必要時に `ToolSearch` で `select:<tool_name>` を呼んでスキーマをロードしてから使う。

3. **切り分け順序**
   1. `claude mcp list` でConnected表示か → サーバー設定OK
   2. （Unity MCP等HTTP経由の場合）`netstat -ano | grep :<port>` でサーバープロセス稼働確認
   3. ここまでOKなら `enabledMcpjsonServers` 漏れを最優先で疑う

## メモリ運用方針

会話中に得た重要な情報は、控えめにせず積極的にメモリへ保存する。ユーザーから「ある程度判断で構築してほしい」「グローバルにも積極的に」と明示的に依頼されているため。

- **グローバル（このCLAUDE.md）**: ユーザーの技術レベル、作業スタイル、全プロジェクト共通の好み・方針を追記する。
- **プロジェクトメモリ（`~/.claude/projects/<project>/memory/`）**: そのプロジェクト固有の構成、採用技術の経緯、進行中の作業文脈を保存する。
- 迷ったらプロジェクト側に保存する（スコープは狭い方が安全）。

### 定期棚卸し（30日サイクル）

メモリは積極保存方針のため放置すると stale 情報が溜まり、判断を歪める原因になる。以下のルールで剪定を回す。

- セッション冒頭で `~/.claude/projects/<project>/memory/_last_pruned.txt` を確認する。最終剪定日から30日以上経過していたら、最初のユーザーメッセージ応答時に「メモリ棚卸しを実施しますか？」と提案する。
- 棚卸しの対象:
    1. 「進行中タスク」「現在のwork-in-progress」系の完了済みエントリ削除
    2. 期限付きエントリ（「YYYY-MM-DDまで」「来週」等）の有効性確認・更新
    3. 重複・矛盾するエントリの統合
    4. ファイル/関数名を含む memory が今も存在するか `grep` 検証（`Before recommending from memory` 原則）
    5. プロジェクト消滅・大幅リネームに伴う obsolete memory の削除
- 剪定実施後、`_last_pruned.txt` に `YYYY-MM-DD` を1行で記録する。ファイルが無ければ作成する。
- 棚卸し中に削除したエントリ数・更新したエントリ数を簡潔に報告する。

## 自作 MCP のソース所在と更新フロー（恒久・全プロジェクト共通）

自作 MCP のソースは以下に存在する。MCP に機能追加・修正が必要になったら、**使用中プロジェクト内の embed パッケージを直接いじるのではなく、必ず下記のソースリポジトリを更新する**。プロジェクト固有 Editor スクリプトで MCP 的な機能（例: 自動 Play、UI キャプチャ等）を暫定実装した場合も、恒久化のため MCP 本体へ移設する。

- **WinGui MCP**: `D:\_Personal\Project\WinGuiMcp`
- **Unity MCP**: ソース所在・更新フローは `~/.claude/rules/unity.md`（Unity プロジェクトで自動注入）を参照。

### WinGui MCP の更新フロー
1. `D:\_Personal\Project\WinGuiMcp` のソースを更新（C# / net9.0-windows、`dotnet build` で検証可）。
2. コミット/Push の自動可否は未確認のため、**Push 前に一度確認する**。
3. 稼働中の win-gui は `D:\_Personal\Project\WinGuiMcp\publish\WinGuiMcp.exe` から起動している。
   デプロイ(publish 上書き)は **MCP プロセスが exe をロックしているため Claude Code を終了してから**行う。
   新ツールの反映には Claude Code 再起動が必要(MCP クライアントは接続時にツール一覧を取得するため)。

## プロジェクト種別ルールの自動注入

プロジェクト種別ごとの固有ルールは `~/.claude/rules/` 配下に分離し、SessionStart hook（settings.json）が該当プロジェクトでのみ context に注入する。

- `rules/unity.md` — Unity プロジェクト（判定: `ProjectSettings/ProjectVersion.txt` の存在）。デバッグフロー・シーン保存・Unity MCP 更新フロー・起動注意を含む。
