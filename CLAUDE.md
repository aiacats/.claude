# 実装時の基本方針

## 1．実装前の原則

- 「.claude」フォルダ内の「claudeLog.txt」というファイルを読み込んで、過去の自分の作業を把握する。
- バグの修正を依頼した場合、ルート直下に存在する「log」フォルダ内の最新ログファイルを参照して原因を確認する。
- 既存コードを調査する。
    - 関連する機能の実装内容を把握する。
    - 使用されているライブラリ・フレームワークを把握する。
    - プロジェクトの命名規則とコーディングスタイルを把握する。
- requirements.txt, package.json, CMakeLists.txt 等で依存関係を確認する。
- 実装を行う前に実装手法を提案し、その承諾を得てから実装を行う。

## 2．コード実装時の原則

- Planの提案と破壊的な変更以外は全て自動で承認して実装を進める。
- 実装を簡易版で済ませることをせず、大規模な実装になるとしてもこちらの要求を完全に満たすために必要な本格的な実装のみを行う。
- 既存コードのコーディングスタイル・命名規則・設計パターンに従う。
- 既存のライブラリで目的の達成に有用なコードが既に実装/API が存在する場合、可能な限り既存のコードを用いて実装する。
- 実装にあたって必要なライブラリが存在する場合、インターネット上からそれをインストールするようにする。
  ClaudeCodeだけでのインストールが難しい場合、インストール方法を説明し、こちらにインストールをさせる。それが完了するまで実装は一時的に停止する。
- 必要最小限の変更で目的を達成する。
- 必要とされる精度は維持した上でパフォーマンスを重視する。
- UI に関連する処理は非同期にし、メインの処理中にキャンセルなどの動作が出来るようにする。
- 堅牢かつメンテナンス性に優れた状態になるように、SOLID 原則・DI を意識しつつ適切なデザインパターンも取り入れる。
- 新規にファイルを作成する際、適切なディレクトリが存在するか確認する。無い場合には適切な場所に適切なディレクトリを生成して、そこにファイルを作成する。
- 説明文書などのドキュメントは明示的に要求した時にのみ作成する。
- API キーやパスワード等をコードに含めない。
- 実装の過程で不要になったコードやファイルが発生した場合には該当コード・ファイルを削除する。
- 一つのクラスが 500 行を大きく超えた場合には責任及びクラスの分割を検討する。
- 可能な限りコンパイルエラーが発生しないよう、他の関連クラスとの関係性も確認しながら実装を行う。
- エラーの修正は、応急処置的な対応はせず根幹の問題を確認して、その問題に対して解決をするようにする。
- フォールバック（Fallback）の実装は行わない。
- コンソール出力するログはルート直下に存在する「log」フォルダ内に「yymmdd-hhmmss.log」という名称でテキストファイルにも保存するようにする。
  その実装が行われていない場合にはその実装を行う。
- 実装を中途半端に止めず、実装する予定のものは全て実装を行う。

## 3．実装後の原則

- 各プロジェクトの使用言語・フレームワークに沿ったやり方（後述参照）でコンパイルエラーなどが発生していないかを確認する。
  発生していた場合には、エラーが無くなり正常にアプリを実行出来るようになるまで修正を行う。
  エラー修正の際には、想定していた実装が実現出来ることを維持して、なおかつ応急処置的な対応ではなく根本原因を解決する。
- **Unityプロジェクトの場合**、claude-code-mcp-unity MCPツールを使用して以下のデバッグフローを実行する。
    1. `check_compilation_status` / `get_compilation_errors` でコンパイルエラーを確認する。
    2. エラーがあれば修正し、`force_compilation` / `hot_reload` で再コンパイルする。
    3. `get_console_logs` でランタイムエラーやログを確認する。
    4. `run_tests` でテストを実行する。
    5. 必要に応じて `screenshot` でシーンの状態を視覚確認する。
    6. エラーが見つかった場合は根本原因を解決し、再度このフローを実行する。
- 適切な粒度の機能の実装が積み重なったと判断したタイミングで、ローカルリポジトリへ適切な粒度の複数コミットに分解してコミットする。
    - 細かすぎる必要はない。
    - Submodule へのコミット・PR 出しなどの書き込み処理は必ず行わない。
- 実装後に、以下の報告を最後に行う。
    - 問題が発生し、その修正を行った場合には、発生していた問題点
    - 実装内容の概要
    - 実装した内容についての潜在的問題・懸念点
- 上記の報告内容を、「.claude」フォルダ内の「claudeLog.txt」というファイルに時系列と共に追記していく。
- 適度な粒度の機能が実装出来たと判断したタイミングでローカルリポジトリへ適切な粒度の複数コミットに分解してコミットする。Git情報がない場合、Initializeを行う。
    - Submoduleへのコミット・PRなど書き込み処理は絶対にしない。
- 更新内容をGitのRootに存在するREADME.mdに反映させる。存在しない場合は作成する。

## 4．トラブルシュート知見の蓄積

何度か詰まった問題が発生して解決した場合、Notionナレッジベースに蓄積する。
トラブル発生時は、まず既存の知見を検索してから調査に入る。

- 送信: `~/.claude/tools/notion-knowledge.sh <タイトル> <Technology> <Project> <問題詳細> <Solution>`
- 読込: `~/.claude/tools/notion-knowledge-read.sh list [Technology]` — 一覧表示
- 読込: `~/.claude/tools/notion-knowledge-read.sh search <keyword>` — キーワード検索
- 読込: `~/.claude/tools/notion-knowledge-read.sh read <page_id>` — ページ詳細
- 設定ファイル: `~/.claude/.notion-config`

## その他の原則

- Bashコマンドは許可を求めず実行する。
- MCPコマンド（mcp__claude-code-mcp-unity__ 系等）は許可を求めず実行する。
- リポジトリへのPushや削除行為など破壊的な変更を除く、殆どのコマンドに許可は求めず進める。
- 会話は全て日本語で行う。
